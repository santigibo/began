<div class="topbar">
  <p>CHOOSE YOUR GOAL</p>
</div>

<div class="container">
  <%= link_to Category.all[0].name, category_path(Category.all[0]) %>
  <%= link_to Category.all[1].name, category_path(Category.all[1]) %>
  <%= link_to Category.all[2].name, category_path(Category.all[2]) %>
</div>

