<h1>All recipes</h1>
<% @recipes.each do |recipe| %>
  <div>
    <%= cl_image_tag recipe.photo.key %>
    <%= recipe.name %>
    <%= recipe.description %>
    <%= recipe.time %>
    <%= recipe.difficulty %>
  </div>
<% end %>
